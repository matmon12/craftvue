import { Meta, Title, Subtitle, TableOfContents, Story } from '@storybook/addon-docs/blocks'
import { CustomSource } from 'docs/components/CustomSource/CustomSource'
import configCodeTexts from './codes'
import * as EmptyStories from './Empty.stories.ts'

<Meta title="Getting Started" />

<div className='sb-docs-wrapper' >
  <div className='sb-docs-content'>

    <Title>Установка CraftVue</Title>

    <Subtitle>Настройка CraftVue в проектах на Vite: установка, подключение и базовая стилизация.</Subtitle>

    &nbsp;

    ## Скачивание

    Библиотеку можно скачать через любой пакетный менеджер

    <CustomSource code={configCodeTexts.download} actions={['copy']} parser="bash" language="apache" />

    &nbsp;

    ## Подключение

    ### Базовое использование

    Для использования компонентов в проекте необходимо их импортировать из подключенной библиотеки
    `craftvue`

    Также компоненты можно импортировать отдельно по мере необходимости, для контроля размера
    подключаемых пакетов.

    <CustomSource code={configCodeTexts.basicUsage} actions={['copy']} />

    ### Глобальная регистрация

    Компоненты можно также регистрировать глобально в `main.ts(js)` и использовать в проекте без
    дополнительного импорта.

    <CustomSource
      code={configCodeTexts.globalReg}
      actions={['copy']}
      parser="babel"
      language="typescript"
    />

    ### Автоимпорт (рекомендуется)

    Для максимального удобства разработки рекомендуется настроить автоимпорт компонентов. Это позволит
    использовать компоненты без явного импорта в каждом файле.

    Установите необходимые плагины и настройте Vite:

    <CustomSource
      code={configCodeTexts.autoImport}
      actions={['copy']}
      parser="babel"
      language="typescript"
    />

    &nbsp;

    ## Стилизация

    По умолчанию компоненты импортируются без стилизации. Стили подключаются отдельно в `main.ts(js)`.

    <CustomSource
      code={configCodeTexts.styles}
      actions={['copy']}
      parser="babel"
      language="typescript"
    />

    <Story of={EmptyStories.Basic} />

  </div>

  <div>
    <TableOfContents
      className='sb-docs-toc sb-unstyled'
      title='Содержание'
      contentsSelector='.sb-docs-content'
      headingSelector="h2, h3"
      ignoreSelector='.sbdocs-subtitle'
    />
  </div>
</div>
