name: CI

# Триггеры для запуска CI
on:
  push:
    branches:
      - main
    # Запуск при изменении файлов, влияющих на сборку
    paths:
      - 'apps/**'
      - 'packages/**'
      - '.github/workflows/CI.yml'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.eslintignore'
      - 'eslint.config.*'
      - '.prettierrc*'
      - 'tsconfig*.json'
      - 'vite.config.*'

  pull_request:
    branches:
      - main
    paths:
      - 'apps/**'
      - 'packages/**'
      - '.github/workflows/CI.yml'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.eslintignore'
      - 'eslint.config.*'
      - '.prettierrc*'
      - 'tsconfig*.json'
      - 'vite.config.*'

jobs:
  # Подготовка окружения и установка зависимостей
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout кода
        uses: actions/checkout@v4

      - name: Установка pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.17.1

      - name: Настройка Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'pnpm'

      - name: Получение пути к pnpm store
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Настройка кэша pnpm
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Установка зависимостей
        run: pnpm install --frozen-lockfile

      - name: Сохранение node_modules
        uses: actions/upload-artifact@v4
        with:
          name: node-modules
          path: node_modules
          retention-days: 1

      - name: Сохранение node_modules/.bin
        uses: actions/upload-artifact@v4
        with:
          name: node-modules-bin
          path: node_modules/.bin
          retention-days: 1

  # Проверка кода линтером
  lint:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - name: Checkout кода
        uses: actions/checkout@v4

      - name: Установка pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.17.1

      - name: Настройка Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'pnpm'

      - name: Восстановление node_modules
        uses: actions/download-artifact@v4
        with:
          name: node-modules
          path: node_modules

      - name: Восстановление node_modules/.bin
        uses: actions/download-artifact@v4
        with:
          name: node-modules-bin
          path: node_modules/.bin

      - name: Исправление прав доступа
        run: |
          echo "Fixing permissions for node_modules/.bin..."
          chmod +x node_modules/.bin/* || echo "Permission fix failed"
          ls -la node_modules/.bin/ | head -5

      - name: Диагностика путей
        run: |
          echo "Current directory: $(pwd)"
          echo "Contents of current directory:"
          ls -la
          echo "Contents of node_modules:"
          ls -la node_modules/ | head -10
          echo "ESLint binary check:"
          ls -la node_modules/.bin/eslint || echo "ESLint not found"
          echo "PATH check:"
          echo $PATH
          echo "Which eslint:"
          which eslint || echo "eslint not in PATH"

      - name: Проверка линтером
        run: npx eslint . --fix

  # Проверка типов TypeScript
  typecheck:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - name: Checkout кода
        uses: actions/checkout@v4

      - name: Установка pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.17.1

      - name: Настройка Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'pnpm'

      - name: Восстановление node_modules
        uses: actions/download-artifact@v4
        with:
          name: node-modules
          path: node_modules

      - name: Восстановление node_modules/.bin
        uses: actions/download-artifact@v4
        with:
          name: node-modules-bin
          path: node_modules/.bin

      - name: Исправление прав доступа
        run: chmod +x node_modules/.bin/*

      - name: Проверка типов
        run: pnpm run type-check

  # Сборка пакетов и Storybook
  build:
    needs: [lint, typecheck, setup]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout кода
        uses: actions/checkout@v4

      - name: Установка pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.17.1

      - name: Настройка Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'pnpm'

      - name: Восстановление node_modules
        uses: actions/download-artifact@v4
        with:
          name: node-modules
          path: node_modules

      - name: Восстановление node_modules/.bin
        uses: actions/download-artifact@v4
        with:
          name: node-modules-bin
          path: node_modules/.bin

      - name: Исправление прав доступа
        run: chmod +x node_modules/.bin/*

      # Сборки
      - name: Сборка всех пакетов
        run: pnpm run build:all

      - name: Сборка Storybook
        run: pnpm run build-storybook
        env:
          NODE_ENV: production

      # Артифакты
      - name: Загрузка артефактов пакетов
        uses: actions/upload-artifact@v4
        with:
          name: packages-artifacts
          path: |
            packages/craftvue/dist
            packages/icons/dist
          retention-days: 1

      - name: Загрузка артефактов Storybook
        uses: actions/upload-artifact@v4
        with:
          name: storybook-artifacts
          path: apps/docs/storybook-static
          retention-days: 1
